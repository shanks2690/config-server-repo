

server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://shanksazuresqlsrvr.mysql.database.azure.com:3306/department_db
    username: azureadmin
    password: Shashank123
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update

  cloud:
    config:
      enabled: false
eureka:
  instance:
    client:
      serverUrl:
        defaultZone: http://localhost:8761/eureka/
